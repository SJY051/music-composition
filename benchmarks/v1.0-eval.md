# v1.0 Benchmark Evaluation

> **Status:** Pending. This file will be populated after the first benchmark run.

The canonical record of skill-on vs. skill-off comparison results, including:

- Per-prompt rubric scores (A / B / C across 5 dimensions)
- Blind judgment per prompt with one-sentence rationale
- Aggregate observations and honest accounting of where the skill helped, was neutral, or didn't help
- Source raw run (`benchmarks/results/v1.0-<timestamp>.{md,jsonl}`) reference

For methodology and how to run the benchmark, see [`README.md`](./README.md).

For the script itself, see [`run_eval.py`](./run_eval.py).
